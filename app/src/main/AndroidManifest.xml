<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jogjaide.ternakapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".anak.MainAnakActivity"></activity>
        <activity android:name=".induk.ViewIndukActivity" />
        <activity android:name=".induk.MainIndukActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UpdatePejantanActivity" />
        <activity android:name=".DeletePejantanActivity" />
        <activity android:name=".AddPejantanActivity" />
        <activity android:name=".ViewPejantanActivity" />
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>